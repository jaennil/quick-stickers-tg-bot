telegram:
  token: "YOUR_BOT_TOKEN"

database:
  driver: "postgres"
  dsn: "postgres://sticker:sticker@postgres:5432/sticker?sslmode=disable"

ocr:
  engine: "paddle"
  # proxy_url: "socks5://127.0.0.1:2080"  # optional: SOCKS5 or HTTP proxy for ocr.space
  space_api_keys:
    - "YOUR_OCR_SPACE_API_KEY"
