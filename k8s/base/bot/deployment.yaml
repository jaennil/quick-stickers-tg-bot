apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bot
  template:
    metadata:
      labels:
        app: bot
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: bot
          image: bot
          env:
            - name: HTTPS_PROXY
              value: socks5://xray.proxy.svc.cluster.local:1080
            - name: AI_INDEX_ENABLED
              value: "true"
            - name: AI_EMBED_MODEL
              value: "gemini-embedding-001"
            - name: AI_VISION_MODEL
              value: "gemini-3.1-flash-lite"
            - name: AI_MIN_SCORE
              value: "0.60"
          ports:
            - containerPort: 8080
              name: metrics
          volumeMounts:
            - name: config
              mountPath: /app/config.yaml
              subPath: config.yaml
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: config
          secret:
            secretName: bot-config
